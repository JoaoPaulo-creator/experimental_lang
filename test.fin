//module main <- need to parse this later

// a function declaration
// env is a function that returns x
let env x = x

 let product_id p =
     match p
     | "carro" then "idProdutoCarro"
     | "moto" then "idProdutoMoto"
     | "pesados" then "idProdutoPesado"
     | _ then "idProdutoBens"
